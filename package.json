{
  "name": "cursor-board",
  "version": "0.1.0",
  "description": "Personal kanban board with Cursor Agent ACP auto-approval",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:bg": "node scripts/start-board.js",
    "deploy": "node scripts/deploy-board.js",
    "probe": "node scripts/acp-probe.js",
    "ui:shot": "node scripts/ui-shot.js",
    "test": "node --test --test-concurrency=2 test/*.test.js"
  },
  "author": "Amadeus",
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^11.9.1",
    "croner": "^9.1.0",
    "express": "^4.21.2",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
